/**
 * Site images — add your own image paths or URLs here when ready.
 *
 * Example:
 *   heroBackground: { src: "/images/hero.jpg", alt: "..." },
 *   aboutFabric: { src: "/images/about.jpg", alt: "..." },
 */
export const siteImages = {
  heroBackground: null as { src: string; alt: string } | null,
  aboutFabric: null as { src: string; alt: string } | null,
} as const;
